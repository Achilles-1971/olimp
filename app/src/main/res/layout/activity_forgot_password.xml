<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Поле для ввода email -->
    <EditText
        android:id="@+id/emailEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите email"
        android:inputType="textEmailAddress"
        android:minHeight="48dp" />

    <!-- Кнопка для отправки кода на email -->
    <Button
        android:id="@+id/requestCodeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Отправить код на email" />

    <!-- Поле для ввода кода из письма (изначально скрыто) -->
    <EditText
        android:id="@+id/codeEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите код из письма"
        android:inputType="number"
        android:minHeight="48dp"
        android:visibility="gone" />

    <!-- Поле для ввода нового пароля (изначально скрыто) -->
    <EditText
        android:id="@+id/newPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите новый пароль"
        android:inputType="textPassword"
        android:minHeight="48dp"
        android:visibility="gone" />

    <!-- Поле для подтверждения нового пароля (изначально скрыто) -->
    <EditText
        android:id="@+id/confirmPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Подтвердите новый пароль"
        android:inputType="textPassword"
        android:minHeight="48dp"
        android:visibility="gone" />

    <!-- Кнопка для сброса пароля (изначально скрыта) -->
    <Button
        android:id="@+id/resetPasswordButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сбросить пароль"
        android:visibility="gone" />

</LinearLayout>
