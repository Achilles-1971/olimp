<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Ввод Email -->
    <EditText
        android:id="@+id/emailEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите email"
        android:inputType="textEmailAddress" />

    <!-- Кнопка запроса кода -->
    <Button
        android:id="@+id/requestCodeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Отправить код на email" />

    <!-- Ввод кода из email -->
    <EditText
        android:id="@+id/codeEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите код"
        android:inputType="number" />

    <!-- Кнопка подтверждения кода -->
    <Button
        android:id="@+id/confirmCodeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Подтвердить код" />

    <!-- Ввод нового пароля (изначально скрыт) -->
    <EditText
        android:id="@+id/newPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите новый пароль"
        android:inputType="textPassword"
        android:visibility="gone" />

    <!-- Подтверждение нового пароля (изначально скрыто) -->
    <EditText
        android:id="@+id/confirmPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Подтвердите новый пароль"
        android:inputType="textPassword"
        android:visibility="gone" />

    <!-- Кнопка сброса пароля (изначально скрыта) -->
    <Button
        android:id="@+id/resetPasswordButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сбросить пароль"
        android:visibility="gone" />

</LinearLayout>
